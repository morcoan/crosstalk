{
  "name": "crosstalk",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A cooperative defusal game for one human and one AI agent, built on WebMCP.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "smoke": "node scripts/smoke.mjs",
    "native": "node scripts/native.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "webmcp-types": "^0.1.5"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
